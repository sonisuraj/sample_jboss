# Generated by Cloud Innovate toolkit
apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.application.name }}"
  labels:
    name: "{{ .Values.application.name }}"
spec:
  type: NodePort
  ports:
    - name: "{{ .Values.application.name }}"
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    name: "{{ .Values.application.name }}"
    
